{
  "name": "mangata-gallo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "postcss css/style.css -o dist/style.css",
    "watch:css": "postcss css/style.css -o dist/style.css -w"
  },
  "devDependencies": {
    "autoprefixer": "^10.x",
    "postcss": "^8.x",
    "postcss-cli": "^10.1.0",
    "postcss-nested": "^7.0.2"
  }
}
